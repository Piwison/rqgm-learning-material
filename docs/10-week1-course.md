# Week 1 教材 · AI Agent 評估的地基

**給非工程背景學習者的第一週。** 六課合計約 15 小時，加上週末總結約 40 分鐘。

---

## 開場：為什麼你讀不下去那篇論文

先說結論：**這不是你的問題，是讀法的問題。**

RQGM 那篇論文是寫給 ML 研究者的。它預設讀者已經熟悉樹搜尋、多臂拉霸機（bandit）、Beta 後驗分佈、自我修改程式碼。它不會停下來解釋這些，因為它的目標讀者不需要。

但這裡有一個關鍵事實：**這篇論文真正的想法，完全不需要那些數學。**

它的核心主張可以用一句話講完：

> 如果你的評分標準永遠不變，被你評分的東西遲早會學會鑽這個標準的漏洞。所以標準也必須進化 —— 但要有紀律地進化，不然你會失去比較基準。

這句話你聽得懂。你在審 spec 的時候早就知道了：如果 RD 摸清了你只看某三件事，他們就只會做好那三件事。

論文剩下的 40 頁，是在回答「**怎麼有紀律地改標準**」這個工程問題。那些數學是手段，不是重點。

### 這週的三條規則

**規則一：你這週不會完整讀那篇論文。** 前四天完全不碰它。第五天才第一次打開，而且只讀四個地方（Lesson 4 會給你精確座標）。

**規則二：所有概念先從你自己的工作裡長出來。** 你每次審 YCM 的 TBD 決議、每次打回一個 YCO 原型，那都是評估。我們要做的不是教你新東西，是把你已經在做的事**寫下來、變成可重複的**。

**規則三：遇到不懂的數學符號，先跳過。** 我會在 Lesson 4 給你一份對照表，把論文裡的符號翻成人話。在那之前，看到公式就跳過，不要停。

---

## 本週地圖

| 課 | 內容 | 時間 | 產出 |
|---|---|---|---|
| Lesson 0 | 先做這件事：採集你自己的判斷 | 30 min | 1 個現成範例 + 你自己補 2 個 |
| Lesson 1 | 評估的兩種看法：看結果 vs 看過程 | 1 hr | 三個案例各勾兩個選項 + 一個填空 |
| Lesson 2 | LLM 當裁判：它會怎麼騙你 | 1.5 hr | 四個情境四選一，全對就過 |
| Lesson 3 | 從 spec 到 rubric：你早就會了 | 1.5 hr | 3 條改寫（+2 條選做），逐條勾四題檢查表 |
| Lesson 4 | 論文閱讀協議 + 符號對照表 | 1.5 hr | 讀完四個關鍵段落 + 三個一句話 |
| Lesson 5 | 環境設置 + 第一隻 Agent + 部署骨架 | 9 hr | spec-judge 報告 + 部署的骨架 |
| 週末總結 | 三題開放性問答 | 40 min | 這週唯一要「寫一段」的地方 |

**六課合計 15 小時，週末總結另外約 40 分鐘。** 建議節奏：Day 1 做 Lesson 0+1，Day 2 做 Lesson 2，Day 3 做 Lesson 3，Day 4 做 Lesson 4，Day 5–7 做 Lesson 5，最後收週末總結。

兩件事先講清楚，這樣你不會在中途以為自己漏做了：

**一、Lesson 0–4 沒有任何一題要你寫段落。** 全部是勾選、四選一、填空，或是改寫既有句子。
每一課的檢核點都設計成「10 秒內能確認自己過了沒有」—— 這本身就是這門課在教的東西。
需要自己組織想法的開放性問答，**全部集中在最後的週末總結作業**。

**二、Lesson 0–4 合計 6 小時，全部是閱讀與寫字，不需要安裝任何東西。**
所有需要動工具的事情都集中在 Lesson 5，而 Lesson 5 佔了整週的 9 小時 —— 也就是**六成的時間
在最後一課**。這個分配是刻意的，也是誠實的：前面五課讀起來很快，真正花時間的是裝設定和部署。
排時間的時候請照這個比例排，不要以為每課都差不多長。

---

# Lesson 0 · 先做這件事（30 分鐘）

**在讀任何東西之前，先產出素材。** 這 30 分鐘的產出會被後面每一課用到，也會變成你 Week 2 的錨點集。

## 先看一個寫好的

**案例 1 我幫你寫好了**，當作格式範例。你不用動它。

```
【案例】TBD-MV-03 的決議
【我的判斷】拒絕
【為什麼 · 第一層】它寫「播放體驗要流暢」，但沒有人能驗證什麼叫流暢。
【為什麼 · 第二層】「流暢」沒有定義閾值。可驗證的版本應該是「切換 MV 時，
首幀出現時間 < 300ms」。我拒絕它，是因為它缺一個數字、或一個能在 10 秒內
用眼睛確認的條件。
```

注意第二層在做什麼：它沒有重複第一層，而是**補上第一層沒說出口的那個判準**
（「要有數字或 10 秒內可確認的條件」）。這就是這一課唯一的動作。

## 動手：再補 2 個

你只要寫 **2 個**，不是 3 個。從這三種裡挑最好想起來的：

- 一條 YCM 的 TBD 決議，你覺得可以收掉 / 或退回去重寫
- 一個 YCO 原型，你 approve 了 / 或打回
- 一份 RD 的 handoff 實作，你驗收通過 / 或退回

**第一層很好寫** —— 就是你當時腦中的那句話，照抄就好，不用修飾。

**第二層卡住的話，用這句話開頭：**

> 「如果換一個人來審，他可能會判不一樣，因為我其實還要求了＿＿＿＿。」

把空格填掉就是第二層。填不出來也沒關係 —— 那代表這一條你的標準本來就很清楚，
在第二層寫「同一個判準，任何人看都會一樣」就可以往下走。

## 這一課在做什麼

你剛剛做的事情有個名字：**把隱性判斷顯性化（externalizing tacit judgment）**。

這是整個 AI Agent 評估的起點。你腦中有一把尺，但那把尺目前只存在你腦中 —— 所以它無法被重複、無法交給機器、無法在你休假時繼續運作。

接下來四週要做的所有事，本質上都是同一件：**把你腦中那把尺搬出來，變成一個有版本、可被檢驗、也可能被推翻的東西。**

> **檢核點：** 範例 1 + 你自己的 2 個 = 3 個案例，每個都有兩層「為什麼」。
> 有的話就往下走。

---

# Lesson 1 · 評估的兩種看法（1 小時）

## 核心概念：看結果，還是看過程

假設你要評估 YCO 的膚況報告 Agent。它做的事情大概是：

```
使用者上傳照片
   → Agent 呼叫膚況偵測 API
   → 拿到「毛孔粗大 0.7 / 泛紅 0.4 / 細紋 0.2」
   → 查產品目錄
   → 生成一份報告
```

你有兩種評估方式：

**方式 A — 看結果（Outcome Evaluation）**
只看最後那份報告。它讀起來合理嗎？建議的產品對得上偵測到的問題嗎？語氣會不會太嚇人？

**方式 B — 看過程（Process / Trajectory Evaluation）**
看它中間做了什麼。它有沒有真的呼叫偵測 API？還是憑照片「看起來像」就編了？它查產品目錄了嗎？還是直接寫出一個目錄裡根本沒有的產品？

## 為什麼這個區分很重要

先自己想 30 秒：**一份報告可以「結果正確但過程錯誤」嗎？**

<br>

答案是可以，而且很常見。舉一個具體的：

> Agent 沒有呼叫偵測 API（可能超時失敗了），但它從照片的整體色調猜「這個人應該有泛紅問題」，然後推薦了一款鎮定精華。**結果剛好對了** —— 那個人確實泛紅。你看報告，完全看不出問題。

只看結果，你會給它 PASS。但這個 Agent 其實是壞的，只是這次運氣好。下次遇到一個膚況不典型的使用者，它就會胡說八道，而且你的評估系統會完全沒有預警。

**這就是為什麼「只看結果」的評估會系統性高估 Agent 的能力。**

## 反過來也會出錯

那全部改成看過程呢？也不行。

> Agent 完美地呼叫了每一個 API、走完每一步，但最後那份報告寫得像醫學論文，使用者根本看不懂。過程全對，結果沒用。

## 實務上怎麼選

| 情境 | 主要看 | 理由 |
|---|---|---|
| 有明確對錯（程式碼能不能跑、數學證明對不對） | 結果 | 結果本身就是硬證據 |
| 沒有唯一正解（報告、文案、spec） | 結果為主 + 少量過程 | 過程無法窮舉，但關鍵步驟要抽查 |
| 除錯 / 找出「為什麼會錯」 | 過程 | 只有過程能告訴你錯在哪一步 |
| 上線後的持續監控 | 結果 + 關鍵步驟旗標 | 過程全記成本太高 |

**你 Week 1 的第一隻 Agent 會是純結果評估**，因為它最簡單。過程評估留到 Week 3。

## 動手：三個案例各勾一個

回到 Lesson 0 那三個案例，**每一個只做兩個動作**：

**動作一 —— 勾一個。** 我當時看的是：

```
□ 結果    □ 過程
```

判斷方式：**如果你當時只看到最終產物就下判斷了，那是結果。如果你去追它中間怎麼來的，
那是過程。** 大部分 spec review 是結果。

**動作二 —— 勾一個。** 如果改看另一邊，我的判斷：

```
□ 會變    □ 不會變
```

就這樣，不用寫理由。

### 勾完看這裡

如果你三個都勾「結果」，而且至少有一個勾「會變」—— **那一條就是你未來最會出事的地方。**
那代表你當時接受或拒絕的依據，其實禁不起「它是怎麼做到的」這個追問。把那一條標記起來，
Lesson 3 挑改寫題目時優先挑它。

三個都勾「不會變」也是合理結果，代表你審的東西本來就是結果導向的。

## 常見誤解

**誤解一：「過程評估比較高級。」**
不是。過程評估比較貴、比較難定義「正確的過程」。很多任務根本沒有唯一正確的過程。它是工具，不是目標。

**誤解二：「看結果 = 看最終輸出的字面內容。」**
不完全是。「結果」指的是**任務有沒有達成**。對編程 Agent 來說，結果是「測試有沒有過」，不是「程式碼看起來漂不漂亮」。

> **檢核點：** 下面這句只有一個空格，填得出來就往下走：
>
> 「只看結果會**高估** Agent 的能力，因為它可能靠＿＿達成正確結果。」
>
> （答案在本課最上面那個膚況報告的例子裡。）

> 要用自己的話完整解釋這件事，留到**週末總結作業**第 1 題。現在不用。

---

# Lesson 2 · LLM 當裁判：它會怎麼騙你（1.5 小時）

## 為什麼要用 LLM 當裁判

你不可能每次都親自看 200 份報告。所以做法是：**寫一份評分標準，讓另一個 LLM 照著標準幫你打分。** 這叫 LLM-as-a-Judge。

好消息是它真的有用。2023 年的一項經典研究發現（Zheng et al.，那篇論文提出了 MT-Bench —— 一個讓人類和 GPT-4 同時評分模型回答、再比對兩者是否一致的基準測試），強模型當裁判時，與人類判斷的一致率超過 80% —— 這個數字大約等同於**兩個人類之間**的一致率。

換句話說：一個好的 LLM 裁判，跟你找另一個同事來審，準確度差不多。

壞消息是它有四種**系統性**偏誤。不是偶爾出錯，是穩定地往同一個方向歪。

## 四種偏誤

### 1. 位置偏誤（Position Bias）

給裁判看兩個答案 A 和 B，問哪個好。**它傾向選第一個。**

換句話說，把同樣兩個答案交換順序再問一次，它可能給出相反的答案。

**怎麼防：** 兩種順序都跑，取平均。如果兩次結論不一致，代表這一題裁判其實沒有判斷力，只是在猜。

### 2. 冗長偏誤（Verbosity Bias）

**它偏好比較長的答案。** 即使長的那個並沒有多說什麼有用的東西。

這個偏誤特別危險，因為它會讓被評估的 Agent 學會「多寫一點就會拿高分」，然後你的 YCO 報告會越來越長、越來越囉唆，而分數持續上升。

**怎麼防：** 在評分標準裡明確寫「簡潔本身是加分項」，或者把長度正規化。更好的做法是用**二元條目**（見下方 rubric 設計），因為二元條目對長度不敏感 —— 「有沒有列出至少兩個偵測到的膚況問題」這題，寫 50 字和寫 500 字的答案是一樣的判定。

### 3. 自我偏好偏誤（Self-Preference Bias）

**它偏好自己家族模型產生的內容。**

這是四種裡最陰險的一種。如果你用 Claude 生成報告、又用 Claude 當裁判，裁判會系統性地給高分。你的 pass rate 會很漂亮，而且完全是假的。

RQGM 論文對這件事有具體測量：最強的基準審查器對 **AI 寫的論文**的接受率，高達對人類寫的論文的 **1.91 倍**。同樣品質，只因為是 AI 寫的就更容易被接受。

**怎麼防：** **用不同家族的模型當裁判。** 這是你這整套系統裡最便宜、效果最大的一條規則。生成用一家，評分用另一家。

### 4. 校準不良（Poor Calibration）

**它說「我 90% 確定」的時候，實際正確率可能只有 60%。** 它的信心程度跟實際準確度對不上。

**怎麼防：** 不要問它「你多有信心」，改問它「這一條有沒有通過，並引用原文證據」。要求引證據，會逼它停止憑感覺。

## 從偏誤反推：rubric 的三條設計規則

上面四種偏誤，其實可以用三條寫作規則擋掉大半。

### 規則一：條目要二元（Binary）

**壞：** 「這份報告的建議是否恰當？」
**好：** 「每一個推薦的產品，是否都對應到報告中明確提到的一個膚況問題？（是／否）」

二元條目不受長度影響、不受語氣影響，而且兩個人（或兩次執行）比較容易得到相同結果。

### 規則二：證據約束（Evidence-Bounded）

要求裁判**只能根據眼前的輸出做判斷，並且引用原文**。

```
輸出格式：
{ "criterion": "每個推薦都對應到偵測到的問題",
  "verdict": "fail",
  "evidence": "報告推薦了『深層修護面膜』，但偵測結果只有毛孔與泛紅，
               沒有任何一項對應到『修護』。" }
```

強制引證據，可以擋掉裁判自己腦補上下文的「幻覺性判定」。

### 規則三：結合式接受（Conjunctive）

**所有條目都通過，才算 accept。** 不要用加權平均。

為什麼？因為加權平均允許「一項嚴重錯誤被幾項小優點抵銷」。一份編造了不存在產品的報告，不該因為語氣親切就及格。

## 動手：挑出唯一合格的改寫

先不要自己寫。**先練「認出來」，比較快。**

原始條目是：「**報告的語氣要親切**」。下面三個改寫，只有一個真的合格。哪一個？

```
A. 報告的語氣要讓使用者感到被關心。
B. 報告中沒有出現「嚴重」「病變」「異常」等醫療警示用語。
C. 報告的親切度評分達到 8 分以上（滿分 10 分）。
```

<br>

**答案：B。**

- **A 不合格** —— 「感到被關心」還是要靠感覺，兩個人會判不一樣。它只是把模糊的詞換成
  另一個模糊的詞。
- **C 不合格** —— 出現了「滿分 10 分」。需要打分數就代表還沒拆解完，違反二元原則。
  而且 8 分跟 7 分的界線在哪，沒有人說得出來。
- **B 合格** —— 有一份明確的字詞清單，可以用搜尋確認，兩個人查會得到同樣結果，
  而且答案只有「有」或「沒有」。

**這三個錯法就是你之後最常犯的三種：換一個模糊詞、改成打分數、以及漏掉可檢查的對象。**

再看兩組，一眼掃過就好：

| 原始（模糊） | 改寫（二元可驗證） |
|---|---|
| 播放體驗要流暢 | 切換 MV 時，首幀出現時間 < 300ms |
| 建議要有針對性 | 每個推薦產品都對應到報告中明確列出的膚況問題 |

**你自己的改寫留到 Lesson 3 做**，那一課會給你四題檢查表當工具。這裡只要能認出 B 就夠了。

## 常見誤解

**誤解：「用最強的模型當裁判就好了。」**
最強的模型一樣有這四種偏誤。強模型只是比較準，不是比較沒偏見。**跨家族**比**用更強的模型**重要。

## 檢核：四個情境，各選一個

不要回頭翻。每題四選一，答案在下面。

**情境 1.** YCO 報告的通過率從 62% 漲到 89%，但客服抱怨量沒下降。你去看報告，發現平均長度從
200 字變成 650 字。

**情境 2.** 你用同一個模型家族生成報告、也用它當裁判。內部通過率 91%，但你請同事人工抽驗
20 份，只有 12 份他覺得可以出。

**情境 3.** 你請裁判比較 A、B 兩版哪個好，它選 A。你把兩版對調位置再問一次，它選了「第一個」
—— 也就是原本的 B。

**情境 4.** 裁判每筆判決都附信心分數。你統計它標「95% 確定」的那些，實際只有 64% 跟你一致。

```
每題選項都是： (a) 位置偏誤  (b) 冗長偏誤  (c) 自我偏好偏誤  (d) 校準不良
```

<br>

**答案與防禦手段：**

| 情境 | 是哪一種 | 怎麼防 |
|---|---|---|
| 1 | (b) 冗長偏誤 | 改用二元條目 —— 二元條目對長度不敏感 |
| 2 | (c) 自我偏好偏誤 | 換一個模型家族當裁判（最便宜、效果最大的一條） |
| 3 | (a) 位置偏誤 | 兩種順序都跑、取平均；翻盤本身就是警訊 |
| 4 | (d) 校準不良 | 不要問信心，改要求逐條判定並引用原文證據 |

**四題全對就往下走。** 錯一題以上，只回頭看那一種偏誤的那一小段，不用重讀整課。

---

# Lesson 3 · 從 spec 到 rubric：你早就會了（1.5 小時）

## 核心洞察

你 Notion 裡那份「Claude Project Scaffold」寫著一句話：

> *State, in one line, how you will VERIFY the work.*

**那句話就是 rubric 的定義。**

一條 spec 的驗收條件，和一條 rubric 條目，是同一個東西的兩個名字：

| Spec 世界 | 評估世界 |
|---|---|
| 驗收條件（Acceptance Criteria） | Rubric 條目（Criterion） |
| TBD-MV-01 已解決 | 這個案例 pass |
| Spec review 打回 | 判定 reject |
| Spec v1.2 | Rubric version 2 |
| Handoff 給 RD | 把 rubric 交給 judge 執行 |

所以你不是在學一個新技能。你是在學**如何把一個你已經每天在做的動作，寫成機器可以重複執行的形式**。

## 兩種寫法，挑一種用熟

### EARS 句型（適合功能規格）

Easy Approach to Requirements Syntax。基本結構：

```
WHEN <觸發條件>, THE <系統> SHALL <可觀察的行為>
```

範例：

```
WHEN 使用者在 MV 播放中切換歌曲,
THE 播放器 SHALL 在 300ms 內顯示新 MV 的首幀。
```

### Given / When / Then（適合流程與 Agent 行為）

```
GIVEN <前置狀態>
WHEN <發生的事>
THEN <應該觀察到的結果>
```

範例：

```
GIVEN 膚況偵測回傳了「毛孔 0.7、泛紅 0.4」
WHEN Agent 生成報告
THEN 報告中每一個推薦產品，都對應到這兩項其中之一
```

**兩種都可以，挑一種用到熟為止。** 混用會讓後面的 rubric 難以維護。

## 四題檢查表（本課的唯一工具）

拿任何一條驗收條件，逐題勾。**勾不下去的那一題，就是要改的地方。**

```
□ 1. 有一個明確的觸發時機（什麼時候要檢查這件事）
□ 2. 要觀察的東西，能在 10 秒內用眼睛或一個指令確認
□ 3. 兩個不同的人來檢查，會得到相同結論
□ 4. 它是二元的（是／否），不需要打分數
```

四題都勾得起來，這條就可以直接當 rubric 條目，不用改寫。

**沒勾滿的話，優先順序是固定的：**

| 沒勾到 | 先做這件事 |
|---|---|
| 第 4 題 | **最優先。** 改成是／否的問法。其他三題通常會跟著解決 |
| 第 2 題 | 加一個數字、一個閾值，或一個能直接看到的畫面條件 |
| 第 1 題 | 在句首補上 `WHEN ...` 或 `GIVEN ...` |
| 第 3 題 | 通常是用詞有歧義。把形容詞換成可以查的對象 |

## 動手：3 條必做，2 條選做（本課主要作業）

打開你的 YCM spec，找已經解決的 TBD 決議。**先做 3 條，做完再決定要不要加。**

**找不到 3 條乾淨的怎麼辦？** 這很正常 —— 真實的 spec 裡，有些 TBD 是在留言串裡收掉的，
有些是在 Slack 講完就沒回填。**任何一句「我當時要求過、而且對方交付後我會檢查」的話都算。**
依序試這三個來源：

1. 已經收掉的 TBD 決議（最理想）
2. **還沒收掉的 TBD** —— 那就用你打算怎麼收它的想法
3. 你在 review 裡打回某個東西時寫的那句話（Lesson 0 那三個案例就是現成的來源）

**不要為了湊數編一條假的。** 假的條目在 Lesson 5 會讓你對照不出任何有用的東西 ——
那一步的價值完全來自「AI 跟真實的你」有落差。

每一條的流程固定四步：

1. 抄下原始決議文字（照抄，不要順稿）
2. 上面四題逐個勾
3. 沒勾滿 → 照上面那張優先順序表改寫成 EARS 或 Given/When/Then
4. 記下沒勾到的題號

排成這樣：

| # | TBD ID | 原始決議 | 四題結果 | 改寫後 |
|---|---|---|---|---|
| 1 | TBD-MV-03 | 播放體驗要流暢 | 1✓ 2✗ 3✗ 4✓ | WHEN 使用者切換歌曲, THE 播放器 SHALL 在 300ms 內顯示首幀 |
| 2 | | | | |
| 3 | | | | |
| 4 | *（選做）* | | | |
| 5 | *（選做）* | | | |

**為什麼是 3 條而不是 5 條：** Lesson 5 的 spec-judge 至少要 3 條才跑得出有意義的對照。
5 條比較好，但 3 條不會讓你卡在這一課。**做滿 3 條就可以往下走**，選做那兩條可以等
Lesson 5 跑完發現不夠再回來補。

> **這份表格是 Lesson 5 的輸入。** 你等一下要把它餵給你的第一隻 Agent。

## 常見誤解

**誤解一：「所有東西都能寫成二元條件。」**
不能。有些東西本質上是程度問題（例如「這段文案吸引人嗎」）。做法不是硬掰成二元，而是**拆解**：把「吸引人」拆成「開頭 15 字內有具體利益」「沒有出現三個以上的形容詞堆疊」等等可檢查的代理指標。

**誤解二：「寫越細越好。」**
不是。一條 rubric 有 5–7 個條目通常剛好。超過 10 條，裁判的一致性會開始下降，而且你自己也維護不動。

> **檢核點：** 表格至少有 3 列填完，每一列都有「四題結果」和「改寫後」。有的話就往下走。

---

# Lesson 4 · 論文閱讀協議（1.5 小時）

**現在，而且只有現在，你可以打開那篇論文了。**

論文位置：https://arxiv.org/abs/2606.26294

> **如果那個連結打不開怎麼辦？**（arXiv 偶爾會擋、或是網址改版）
>
> **不要因為這個卡住整週。** 這個 repo 裡有 `docs/00-research-report.md` —— 那是從論文
> 原文整理出來、已經核對過的筆記。它的 **§1（事實核對）** 和 **§2（概念階梯）** 涵蓋了
> 下面「第一次掃描」要你拿到的全部東西。
>
> **退路的正確用法：** 用那份筆記完成 Lesson 4 和週末總結第 2 題，然後在筆記本上記一句
> 「我還沒讀過原文」。等連結能開的時候再補第三次掃描。
>
> **但如果連結能開，就讀原文。** 二手筆記會夾帶 —— 這件事下面就會講到，而且是這一課
> 最重要的一段。

## 為什麼要等到現在

因為你現在有了三樣東西，是四天前沒有的：

1. 你自己的判斷素材（Lesson 0）
2. 結果 vs 過程的分辨能力（Lesson 1）
3. 對 judge 偏誤的認識（Lesson 2）

論文裡有一半的內容，是在解決你**已經知道存在**的問題。有了這三樣，你讀的時候會不斷有「喔，這個就是在講那件事」的感覺 —— 那才是有效的閱讀。

## 三次掃描法

不要從第一頁讀到最後一頁。分三次，每次目標不同。

### 第一次掃描（20 分鐘）· 只求知道「它在主張什麼」

**只讀這些：**
- Abstract
- Introduction（§1）
- **所有圖的標題與說明文字（figure captions）** ← 很多人跳過，但這是資訊密度最高的地方
- Conclusion

**讀完要能回答：**
- 這篇論文說「現有做法」有什麼問題？
- 它提出的解法，一句話是什麼？
- 它宣稱改善了什麼？改善多少？

**不要做的事：** 不要停下來查任何一個看不懂的名詞。第一次掃描的目的是取得地圖，不是理解細節。

### 第二次掃描（40 分鐘）· 知道「每一節在做什麼」

**做法：** 把目錄抄下來，每一節旁邊寫一句話「這節在解決什麼問題」。

你可以只讀每節的第一段和最後一段，通常就夠了。

**遇到公式：跳過。** 真的，跳過。你要的是那節的**主張**，不是推導。

### 第三次掃描（30 分鐘）· 只深讀四個地方

這四個地方是你這四週真正會用到的。其他都可以先不管。

| 讀哪裡 | 你要拿走什麼 |
|---|---|
| **Controlled Utility Evolution 那節** | 「紀元」是什麼、為什麼要在邊界才換評估器 |
| **Selective Erasure 那節 + Figure 3** | 為什麼不擦除舊分數，搜尋會被舊排名「釘死」（ρ ≥ 0.90） |
| **§5 對抗性除偏 + Figure 6** | AI 裁判過度接受 AI 內容，以及怎麼修 |
| **四個 Anchor 的介紹段落** | Polyglot / APReS / IMO-GradingBench / CRAVE 各自在測什麼 |

## 符號對照表：把數學翻成人話

論文裡的符號，這樣讀：

| 符號 | 唸法 | 人話 |
|---|---|---|
| `a` | agent | 存檔裡的**一個版本** |
| `w_a` | workspace of a | 那個版本的**工作空間**（程式碼 + 提示詞） |
| `E_m` | evaluator m | **第 m 版的評分標準／裁判** |
| `S_a`, `F_a` | successes, failures | 那個版本的**成功次數、失敗次數** |
| `CMP(a)` | clade metaproductivity | 「這個版本**連同它所有後代**的總成功率」 |
| `BB_ε(a)` | epsilon best-belief | 「**保守估計**：我有 95% 把握，真實成功率至少有這麼高」 |
| `I⁻¹_ε(·,·)` | inverse regularized incomplete beta | 就是上面那個保守估計的計算工具。**不用懂，會用就好** |
| `N_t^α ≥ \|T_t\|` | expansion gate | 「什麼時候該長出新版本，而不是繼續測舊的」 |
| `Erase_m` | erase operator | 「把跟舊裁判有關的分數作廢」 |
| `ρ`（在 Spearman 那裡） | rho | **排名相關係數**。1.0 = 排名完全沒變 |
| `ρ`（在檢查點那裡） | rho | **完全不同的東西** —— 檢查點的間距倍數（論文用 2） |

> ⚠️ 最後兩列請特別注意：**論文用 `ρ` 表示兩個完全不同的東西**。看到 ρ 要先確認上下文。這種一符多義在論文裡很常見，不是你看錯。

## 這篇論文的可信度：讀之前先知道

這件事很重要，避免你把不該當真的東西當真：

**這是一篇 2026 年 6 月的 preliminary preprint（作者自己這樣定義）。** 單次執行、搜尋視野短。它的**概念**是可靠的，但**所有數字都應該視為暫定**。

另外，你手上那份中文摘要有兩個地方是錯的：

- **「13.0 倍成本削減」在論文裡不存在。** 那是虛構的數字。真實最大倍數約 3 倍。
- **「Nemotron 3 Ultra」名稱錯誤。** 論文用的是 Nemotron 3 Super 120B，只出現在附錄的消融實驗，而且論文明講那個路由「從未被觀察到實際發生」。

**這件事本身就是一課：** 二手摘要會夾帶。你剛學的「證據約束審查」原則 —— 只根據眼前的原文做判斷、要求引證據 —— 第一個該用在你自己的閱讀上。

## 動手：三個一句話

三次掃描做完後，**不要寫段落。** 把下面三句各補完一句話就好，每句 30 字內：

```
1. RQGM 想解決的問題是：＿＿＿＿＿＿＿＿＿＿

2. 它的解法是：＿＿＿＿＿＿＿＿＿＿

3. 我要拿走用在自己工作上的一個想法是：＿＿＿＿＿＿＿＿＿＿
```

**第 1、2 句寫不出來，代表第一次掃描沒掃到位** —— 回去重看 abstract 和圖說，20 分鐘就好。

**第 3 句沒有標準答案**，寫任何一個你真的打算做的事都算過。如果想不到，直接用這個：
「把我的 spec 驗收條件改成二元的，並且固定用非 Claude 的模型來判。」

> **檢核點：** 三句都有東西，而且沒有一句是照抄論文原文。

> 完整的 150 字摘要留到**週末總結作業**第 2 題。現在不用寫。

---

# Lesson 5 · 環境設置 + 第一隻 Agent + 部署骨架（9 小時）

這一課是動手，不是概念。

## Part A · 環境（1 小時）

三件事，照順序做。**第 3 件最容易被跳過，但少了它 Part B 完全跑不起來。**

1. **Supabase**（放資料的地方）— 到 supabase.com，把你其中一個暫停的專案恢復（或開新的，選 ap-southeast 區）。到 Settings → API 拿 `Project URL` 和 `anon public key`，存到記事本。

2. **Vercel**（部署用）— 到 vercel.com 確認你能登入。這步先不用建專案。

3. **Judge 的 API key** — **這是 Part B 的必要條件，不是選配。**

   到 **platform.openai.com** → API keys → 建一把新的。需要綁一張信用卡。

   > **為什麼不能用 Claude？** 因為規則一：判定的模型必須跟生成的模型不同家族。
   > 你整週都在用 Claude Code 寫東西，所以判定端不能也是 Claude —— 那會系統性地
   > 給自己高分（Lesson 2 的自我偏好偏誤）。

   **會花多少錢：** 這週你只會判 8–12 條短文字。**總花費會在 1 美元以下**，通常是幾美分。
   不用擔心，但也不要把 key 貼到任何公開的地方。

   拿到 key 之後，把 repo 根目錄的 `.env.example` 複製成 `.env`，填進 `JUDGE_API_KEY`。
   `JUDGE_MODEL` 填你在 OpenAI 頁面上看到的當前模型代號 —— **不要照抄任何教材裡寫死的
   模型名稱**，那些過期得比這門課還快。

   想用 Gemini 也可以（aistudio.google.com，有免費額度），一樣不違反規則一。

**Notion 呢？** 這週不用設。Notion 同步是 **Week 2** 的內容，而且 Week 1 的產出完全不碰它 ——
現在去設 integration，你的 Tasks 頁面這週還是會是空的，只是白花 15 分鐘。等到需要的那一課
會告訴你怎麼做。

### 還有一件沒人會告訴你的事

Part B 和 Part C 都需要你的電腦上有 **Node.js**（跑 JavaScript／TypeScript 的東西）。

你可能已經有了。**不用先去裝** —— 直接往下做，如果缺，Claude Code 會告訴你並要求你同意安裝。
看到它要跑 `npm install` 或類似的東西，**同意就對了**，那是正常流程，不是它在亂裝東西。

**在此之前先講清楚，是為了讓你看到那個畫面時不會停下來懷疑自己踩到什麼陷阱。**

## Part B · 第一隻 Agent：Spec-Resolution Judge（3 小時）

**這隻 Agent 只做一件事：** 判斷你的 spec 決議有沒有包含「可在 10 秒內驗證的條件」。

它會用到你 Lesson 3 做的那份五列表格 —— 但這次是讓 AI 來判，然後你去對照它跟你的判斷差多少。

### 開始前先想 60 秒

在跑之前，先自己預測：**你那 5 條決議裡，你覺得 AI 會判幾條 FAIL？**

寫下數字。等等對照。這個動作叫做預測 - 對照，是讓學習真正發生的關鍵步驟 —— 只有在你的預測被打臉的時候，你才會真的記住。

### 執行

把下面整段 prompt 貼進 Claude Code，然後把你 Lesson 3 的表格內容也貼給它。

（`web/rqgm-console.html` 的「工具包」分頁裡有同一段，附複製按鈕。用哪邊都一樣。）

```
GOAL: Build a small evaluation agent called "spec-judge" that grades whether my
resolved YCM spec items actually meet a verifiable acceptance criterion. I drive
Claude Code and review code — keep it a single readable TypeScript script, no framework.

INPUT: I will paste 8-12 of my real resolved TBD-items. Each has: an ID
(e.g. TBD-MV-01), the resolution text, and (if present) an acceptance/verification line.

WHAT TO BUILD:
1. A script `spec-judge.ts` that takes a JSON array of items and, for each, asks an
   LLM judge to decide PASS/FAIL on this single question: "Does this resolution
   contain a criterion that a reviewer could verify in under 10 seconds by looking
   at the product or code?"
2. The judge must output strict JSON: { id, verdict: "PASS"|"FAIL",
   reason: "<one sentence>", suggested_criterion: "<if FAIL, a rewrite that WOULD
   be verifiable, in EARS or Given/When/Then form>" }.
3. Use a model from a DIFFERENT family than whatever wrote the specs, to reduce
   self-preference bias. State which model you're using and why.
4. Print a summary table: PASS count, FAIL count, and the list of suggested_criteria
   for the fails.
5. Save the output to `spec-judge-report.md`.

CONSTRAINTS:
- Do NOT touch any other files. This is a standalone artifact.
- Before writing code, restate in ONE line how you will VERIFY the script works,
  then write a 2-line sample input and show me the expected output shape.

ACCEPTANCE CRITERIA (my rubric for YOUR work):
- The script runs on my pasted items without errors.
- Every FAIL comes with a concretely better, verifiable criterion.
- The report is a markdown file I can skim in 60 seconds.
```

### 執行後最重要的一步

**不要只看報告就收工。** 做這件事：

1. 你的預測是幾條 FAIL？實際是幾條？
2. 逐條對照：**AI 的判斷和你的判斷，哪幾條不一樣？**
3. 對每一條不一樣的，問自己：**是 AI 錯了，還是我的標準其實沒說清楚？**

第 3 題是這整週最有價值的一題。

如果 AI 判 FAIL 而你覺得該 PASS，通常代表**你腦中有一個沒寫出來的補充條件**。把它挖出來寫下來 —— 那就是你 rubric 缺的那一條。

### 把結果記進台帳

打開 `web/rqgm-console.html`（直接用瀏覽器開）→ 台帳分頁：

- 每一條決議加一筆案例（代號用 TBD ID）
- 「我的判定」填你自己的判斷
- 「J 接受 / J 拒絕」填 AI 的判斷
- 你最有把握的那幾條，標成**錨點**

記完之後，首頁的 κ 就會亮起來。**那個數字就是「AI 這把尺跟你這把尺有多像」。**

### 先講清楚：這週的 κ 幾乎一定很難看

**只有 8–12 筆、而且判定大多集中在同一邊的時候，κ 這個數字本來就會怪。** 你可能會看到：

| 你看到 | 意思 |
|---|---|
| **0 或接近 0** | 不代表「完全不一致」。代表「你們的一致程度跟隨便猜差不多」 |
| **負數** | 也是正常的。代表比隨便猜還差 —— 在 8 筆樣本下這很容易發生 |
| **空白或算不出來** | 你的判定全部都是同一邊（例如 8 筆全 PASS）。κ 需要兩邊都有東西才算得出來 |

**這三種情況都不代表 judge 壞掉，也不代表你做錯了。** 它們代表樣本太少。

κ 要到 20–30 筆才開始有意義 —— 那是 Week 2 的事。
**這週的目標只有一個：看到那個數字出現，知道它從哪裡來。** 數字本身現在不重要。

---

## Part C · 把骨架部署起來（5 小時）

**這一段沒有新概念，純粹是把地基蓋好。** Week 3 的儀表板要長在這個骨架上，早點蓋完，
後面每一週都省事。

### 先回答一個問題：這個專案放在哪裡

**答案：放在這個 repo 裡面，開一個 `worktracker/` 資料夾。** 不要另開一個新的 GitHub repo。

```
rqgm-learning-material/
├── docs/            ← 教材
├── web/             ← 學習工具
├── supabase/        ← schema.sql
└── worktracker/     ← ★ Part C 要建的 Next.js 專案就放這裡
```

**為什麼講這個：** 因為 Next.js 的教學文件通常假設你的專案就是整個 repo 的根目錄，
而你的不是。這會影響一個 Vercel 設定，如果不知道，你會卡在「部署成功但打開是 404」。

**那個設定是：** 在 Vercel 建專案時（或之後到 Project Settings → General），
把 **Root Directory** 設成 `worktracker`。**只有這一個地方要改。**

漏了這一步的症狀很好認：Vercel 說 build 成功，但網址打開是 404 或空白。
看到那個症狀，回來改這個設定就好。

要做出來的東西只有三頁，而且**都不寫業務邏輯，只做讀取**：

| 頁面 | 內容 | 空資料時 |
|---|---|---|
| `/` | `tasks` 表的清單 | 顯示空狀態，不是錯誤 |
| `/runs` | `agent_runs` 表的清單 | 顯示空狀態，不是錯誤 |
| 部署 | Vercel 上有一個可以打開的網址 | — |

### 先套用 schema

`supabase/schema.sql` **已經寫好了**，五張表加一個 `v_current_runs` view。

到 Supabase → SQL Editor，把那個檔案整份貼進去執行。**不要讓 AI 幫你重新產生一份
schema** —— 現有這份已經包含了 `stale`（選擇性擦除）和 `is_anchor`（錨點）這些
你 Week 4 才會用到、但現在漏掉就要重做的欄位。

### 執行

```
GOAL: Create a Next.js 15 App Router + TypeScript project in a NEW `worktracker/`
subdirectory of this existing repo. Do not create a separate repo, and do not move
or touch docs/, web/, supabase/ or .claude/.
Use the Supabase JS client. I will paste SUPABASE_URL and SUPABASE_ANON_KEY into
.env.local. I drive Claude Code and review code — keep it simple and readable.

IMPORTANT: The database schema already exists at `supabase/schema.sql` (5 tables +
a v_current_runs view) and I have already applied it. Do NOT write a new schema.
Read that file and match your queries to the columns that are actually there.

WHAT TO BUILD:
1. A `/` page listing rows from `tasks` (id, title, status, priority, source,
   updated_at). If the table is empty, show an empty state — not an error.
2. A `/runs` page listing rows from `agent_runs` (id, agent_name, verdict, score,
   model_used, tokens_in, tokens_out, cost_usd, created_at).
3. Deploy to Vercel.

CONSTRAINTS:
- No application logic beyond CRUD reads. No auth. No styling framework.
- Before you start, restate in ONE line how you will VERIFY each of the 3 items.
  Then implement. After each item, run it and show me the result.
```

### 為什麼這裡要花 5 小時

因為第一次串 Supabase 的環境變數、和第一次上 Vercel，幾乎一定會卡一次。
**卡住是正常的，不是你笨。** 這 5 小時裡大概有 1.5 小時是在踩坑，那是預算的一部分。

三個坑，連「去哪裡修」一起寫給你：

**坑 1 · 環境變數名稱前綴**
`worktracker/.env.local` 裡的變數，如果要讓瀏覽器端讀到，名稱必須以 `NEXT_PUBLIC_` 開頭
（例如 `NEXT_PUBLIC_SUPABASE_URL`）。沒有前綴的變數只有伺服器端讀得到。
**症狀：** 本機跑起來但頁面顯示 undefined 或抓不到資料。

**坑 2 · Vercel 要再設一次環境變數**
本機的 `.env.local` **不會**跟著上傳（它被 gitignore 了，這是對的）。
**去哪裡設：** Vercel → 你的專案 → **Settings → Environment Variables** → 一個一個加，
然後**重新 deploy 一次**（改環境變數不會自動觸發重新部署，這是最常見的第二次卡住）。

**坑 3 · Root Directory**
就是上面說的那個。**去哪裡設：** Vercel → 專案 → **Settings → General → Root Directory**
→ 填 `worktracker`。

如果 Claude Code 用 CLI 幫你部署，它第一次會需要你在瀏覽器完成 `vercel login`
—— 那個瀏覽器跳窗是正常的。

> **檢核點：** 你有一份 `spec-judge-report.md`，台帳裡有至少 8 筆案例，首頁 κ 有數字，
> 而且 Vercel 上有一個打得開的網址，`/` 和 `/runs` 都顯示空狀態而不是報錯。

---

# 週末總結作業

**這一節是這週唯一需要「自己寫一段」的地方。** 前面每一課都刻意做成勾選、選擇、填空，
理由是：那些地方的目的是讓你往下走，不是讓你卡住。到了這裡你已經有素材了，寫起來會
比週一那時候容易得多。

三題，總共約 40 分鐘。**寫完就是 Week 1 結束。**

### 第 1 題（10 分鐘）· 解釋給別人聽

用你自己的話，向一個沒學過這些的同事解釋：

> **為什麼「只看結果」的評估會系統性高估 Agent 的能力？**

規則：**要舉一個你自己工作裡的例子**，不要用課程裡的膚況報告。YCM、YCO、RD handoff 都可以。

寫不出例子的話，這題的正確處理是回去看 Lesson 1 那個「API 超時但猜對了」的段落，
然後問自己「我審過的東西裡，有哪一個可能是這樣過關的」。

### 第 2 題（20 分鐘）· 150 字論文摘要

不看論文，用自己的話寫 **150 字以內**，回答：

> RQGM 想解決什麼問題？它的解法是什麼？我打算從中拿走哪一個想法用在我的工作上？

你在 Lesson 4 已經寫過三個一句話版本了 —— 把它們展開成段落就好，不要從零開始。

**檢查一件事：有沒有任何一句是論文原文的中文直譯？** 有的話重寫那一句。這一題檢查的
是理解，不是記憶。

### 第 3 題（10 分鐘）· 你的第一條不確定

寫下**一件你這週學到、但還不太相信的事**，以及你打算怎麼驗證它。

例：「課程說跨家族判定比用更強的模型重要。我不確定差距有多大 —— Week 3 做成本路由的時候，
我可以同時記兩種設定的 κ 來比。」

**這一題最有價值。** 它是你 Week 2 的第一個實驗題目，而且它會讓你在後面三週裡保持
「我在檢驗這套方法」而不是「我在照著做」的姿態 —— 那正是這整門課想教的東西。

---

# 本週自我檢核

不要回頭翻。先答，再對答案。

1. 為什麼「只看結果」的評估會系統性**高估** Agent 的能力？
2. LLM 裁判的四種偏誤是哪四種？
3. 四種偏誤裡，哪一種的防禦手段最便宜、效果最大？那個手段是什麼？
4. 為什麼 rubric 要用「結合式接受」（全部通過才算過），而不是加權平均？
5. 一條可驗證的驗收條件，要通過哪四題檢查？
6. `CMP(a)` 用人話講是什麼？
7. `BB_ε(a)` 用人話講是什麼？它為什麼比原始通過率好？
8. 讀論文的三次掃描，每一次的目標分別是什麼？
9. 你手上那份中文摘要有哪兩個地方是錯的？
10. 「選擇性擦除」如果不做，會發生什麼事？

---

## 答案

1. 因為 Agent 可能靠運氣或跳過必要步驟達成正確結果。只看最終產物，你看不出它的過程是壞的，直到遇到邊界案例才爆炸。
2. 位置偏誤、冗長偏誤、自我偏好偏誤、校準不良。
3. **自我偏好偏誤**。手段是**用不同模型家族當裁判** —— 生成用一家、評分用另一家。
4. 因為加權平均允許嚴重錯誤被小優點抵銷。一份編造不存在產品的報告，不該因為語氣好就及格。
5. (a) 有明確觸發時機 (b) 10 秒內可用眼睛或一個指令確認 (c) 兩個人檢查會得到相同結論 (d) 是二元的。
6. 「這個版本連同它所有後代的總成功率」—— 判斷一個版本要看它的家族表現，不是它自己的分數。
7. 「我有 (1−ε) 的把握，真實成功率至少有這麼高」的保守下限。比原始通過率好，是因為它不會被小樣本騙 —— 1 勝 0 敗的「100%」在保守估計下只有約 22%。
8. 第一次：知道它在主張什麼（abstract + intro + 圖說 + conclusion）。第二次：知道每一節在做什麼。第三次：只深讀你會用到的四個地方。
9. (a)「13.0 倍成本削減」是虛構的，論文中不存在。(b)「Nemotron 3 Ultra」名稱錯誤，實際是 Nemotron 3 Super 120B，且論文說該路由從未實際發生。
10. 搜尋會被舊排名「釘死」（Spearman ρ ≥ 0.90），新的、更嚴格的評分標準無法重新排序任何東西 —— 換了尺等於沒換。

**答對 8 題以上：** 可以進 Week 2。
**答對 5–7 題：** 回去重看答錯的那一課的「核心概念」段落，30 分鐘就好。
**答對 4 題以下：** 大概率是 Lesson 1 和 2 讀太快。這兩課是後面所有東西的地基，值得重來一次。

---

# 你這週結束時應該能說出的五句話

如果有人問你「你在學什麼」，你應該能不看筆記講出這五句：

1. **評估有兩層：結果和過程。只看結果會高估 Agent 的能力，因為它可能靠運氣對。**
2. **LLM 可以當裁判，準確度接近人類之間的一致率，但它有四種系統性偏誤 —— 其中最危險的是它偏袒自己家族的產出。**
3. **好的評分條目是二元的、要求引用證據的、而且必須全部通過才算過。**
4. **我寫 spec 的驗收條件，跟寫 rubric 是同一件事。我已經有這個技能，只是沒有把它變成可重複執行的形式。**
5. **RQGM 的核心主張是：固定的評分標準遲早會被鑽漏洞，所以標準必須有紀律地演化 —— 用錨點守住方向，用紀元守住比較基準。**

第 5 句是這整四週的中心。如果只能記一句，記這句。

---

# 下週預告

Week 2 你會把這週的臨時判斷，變成**有版本、可重跑、有錨點守著**的東西：

- 寫出你的第一份正式 rubric（JSON、5–7 條二元條目）
- 把評估案例擴充到 20–30 條，其中約 10 條標成錨點
- 讓 `npm run eval` 一鍵跑完全部
- **硬性關卡：錨點集上的 Cohen's κ 要 ≥ 0.6**

那個 0.6 是整個計畫最重要的一個數字。低於它，代表你的裁判跟你的判斷對不上 —— 那時候後面所有的儀表板、成本優化、紀元轉換全都建在流沙上。

Week 2 有一半的時間，會花在把它拉上去。
